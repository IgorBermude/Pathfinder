<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="6dp"
    app:cardUseCompatPadding="true"
    app:cardCornerRadius="6dp"
    app:cardElevation="6dp"
    app:cardBackgroundColor="?attr/colorSurface">

    <LinearLayout
        android:id="@+id/container_item"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?attr/selectableItemBackground"
        android:padding="10dp"
        android:descendantFocusability="blocksDescendants"> <!-- evita que filhos capturem clique/foco -->

        <ImageView
            android:id="@+id/iv_icone"
            android:layout_width="45dp"
            android:layout_height="50dp"
            android:layout_gravity="center_vertical"
            android:layout_marginEnd="12dp"
            android:importantForAccessibility="no"
            android:contentDescription="@string/ic_icone_desc"
            android:src="@drawable/ic_route_overview"/>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_nome"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="4dp"
                android:ellipsize="end"
                android:fontFamily="sans-serif-medium"
                android:maxLines="1"
                android:text="@string/rota_exemplo"
                android:textColor="?attr/colorOnSurface"
                android:textSize="18sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tv_distancia"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:alpha="0.8"
                android:text="@string/distancia_exemplo"
                android:textColor="?attr/colorOnSurface" />

            <TextView
                android:id="@+id/tv_dt_modificacao"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:alpha="0.6"
                android:text="@string/modificada_exemplo"
                android:textColor="?attr/colorOnSurface"
                android:textSize="12sp"
                android:visibility="gone" />

            <Button
                android:id="@+id/btn_remover"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end"
                android:layout_marginTop="8dp"
                android:backgroundTint="?attr/colorPrimary"
                android:paddingHorizontal="16dp"
                android:paddingVertical="8dp"
                android:text="@string/remover"
                android:textAllCaps="false"
                android:textColor="?attr/colorOnPrimary"
                android:focusable="false"
                android:focusableInTouchMode="false" /> <!-- impede que o botão roube foco/click -->
        </LinearLayout>
    </LinearLayout>

</androidx.cardview.widget.CardView>